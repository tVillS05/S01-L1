print("Quantos números?")
qtd = tonumber(io.read())
numeros = {}

for i = 1, qtd do
    numeros[i] = tonumber(io.read())
end

maior = numeros[1]
for i = 2, qtd do
    if numeros[i] > maior then
        maior = numeros[i]
    end
end

print("Maior = " .. maior)
